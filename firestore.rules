rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lists/{listId}/items/{itemId} {
      // Anyone who knows the list code can read/write its items.
      // Keep your list code private
      allow read, write: if true;
    }
  }
}
